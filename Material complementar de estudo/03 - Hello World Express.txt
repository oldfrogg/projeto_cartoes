// após instalar node e express

import express from "express";

const app = express();

const PORT = 3000;

app.get("/", (req,res) => {
    res.status(200).send("Olá!");
});


app.listen(PORT, () => {
    console.log("Servidor escutando...");
});


export default app;